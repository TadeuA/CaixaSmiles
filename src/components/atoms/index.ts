import AtomsProps from './interface';
import DefaultButton from './DefaultButton';
import Input from './Input';
import BookmarkButton from './BookmarkButton';

const Atoms: AtomsProps = {
  DefaultButton,
  Input,
  BookmarkButton,
};

export default Atoms;
